/** Breakpoints aligned with tokens.css — use in JS media queries. */
export const breakpoints = {
  sm: 600,
  md: 768,
  lg: 980,
  xl: 1440,
} as const;

export const sectionPadding = "clamp(80px, 11vw, 140px)";
