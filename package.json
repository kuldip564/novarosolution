{
  "name": "express-vercel",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "scripts": {
    "vercel-build": "npm --prefix server run vercel-build",
    "build": "npm --prefix clinte run build",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix clinte run dev"
  }
}
