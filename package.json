{
  "name": "novaro-solution",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "concurrently -k -n frontend,backend -c cyan,magenta \"npm run start --prefix frontend\" \"npm run start --prefix backend\"",
    "clean": "npm run clean --prefix frontend",
    "db:seed": "npm run db:seed --prefix backend",
    "sync:team-photos": "bash scripts/sync-team-photos.sh"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
